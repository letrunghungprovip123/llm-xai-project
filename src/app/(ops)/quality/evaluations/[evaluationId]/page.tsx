"use client";
import { useParams } from "next/navigation"; import { PageHeader } from "@/components/common/page-header"; import { StatusTag } from "@/components/common/status-tag"; import { ResourceId } from "@/components/common/resource-id"; import { JsonView } from "@/components/common/json-view"; import { KvGrid } from "@/components/common/kv-grid"; import { QueryError,PanelSkeleton } from "@/components/common/states"; import { dateTime } from "@/lib/format"; import { useGateEvaluation } from "@/lib/query/researchops-hooks";
export default function EvaluationPage(){const {evaluationId}=useParams<{evaluationId:string}>();const id=decodeURIComponent(evaluationId);const q=useGateEvaluation(id);if(q.isLoading)return <PanelSkeleton rows={8}/>;if(q.isError||!q.data)return <QueryError error={q.error}/>;const e=q.data;return <div className="space-y-5"><PageHeader eyebrow="Certified evaluation" title={<span className="inline-flex items-center gap-3">{e.gate_id}<StatusTag value={e.outcome}/></span>} description={<ResourceId value={e.id}/>}/><div className="panel px-5 py-4"><KvGrid items={[["Scope",`${e.scope_type}:${e.scope_id}`],["Severity",e.severity],["Blocking",e.blocking?"Yes":"No"],["Adapter",`${e.adapter_id} v${e.adapter_version}`],["Policy",`${e.policy_id} ${e.policy_version}`],["Payload SHA",<ResourceId value={e.payload_sha256}/>],["Evaluated",dateTime(e.evaluated_at)]]}/></div><div className="grid gap-4 xl:grid-cols-2"><section className="panel p-4"><h2 className="mb-3 text-sm font-semibold">Expected</h2><JsonView value={e.expected}/></section><section className="panel p-4"><h2 className="mb-3 text-sm font-semibold">Observed</h2><JsonView value={e.observed}/></section><section className="panel p-4"><h2 className="mb-3 text-sm font-semibold">Checks</h2><JsonView value={e.checks}/></section><section className="panel p-4"><h2 className="mb-3 text-sm font-semibold">Evidence & limitations</h2><JsonView value={{source_artifact_id:e.source_artifact_id,source_manifest_sha256:e.source_manifest_sha256,evidence_artifact_id:e.evidence_artifact_id,evidence_manifest_sha256:e.evidence_manifest_sha256,source_contract:e.source_contract,limitations:e.limitations,source_contracts:e.source_contracts,source_evaluation_ids:e.source_evaluation_ids}}/></section></div></div>}
