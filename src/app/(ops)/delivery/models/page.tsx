"use client";
import { useRouter } from "next/navigation"; import { Button, Input } from "antd"; import { Search } from "lucide-react"; import { useState } from "react"; import { PageHeader } from "@/components/common/page-header";
export default function ModelsLanding(){const router=useRouter();const [name,setName]=useState("");return <div className="space-y-6"><PageHeader eyebrow="Deliver" title="Model registry" description="FastAPI exposes versions by model name but intentionally does not expose a global model-list endpoint. Enter an exact registered model name; the UI does not fabricate client-side discovery."/><section className="panel mx-auto max-w-2xl p-6"><h2 className="text-lg font-semibold">Open registered model</h2><p className="mt-2 text-sm text-[var(--muted)]">Examples should come from your own model registration receipts or release metadata.</p><div className="mt-5 flex gap-2"><Input value={name} onChange={e=>setName(e.target.value)} onPressEnter={()=>name.trim()&&router.push(`/delivery/models/${encodeURIComponent(name.trim())}`)} prefix={<Search size={14}/>} placeholder="Exact MLflow registered model name"/><Button type="primary" disabled={!name.trim()} onClick={()=>router.push(`/delivery/models/${encodeURIComponent(name.trim())}`)}>Open</Button></div></section></div>}
