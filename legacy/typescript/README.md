# TypeScript pipeline lịch sử

Các module Batch I v1, J1/J2/J3, K, integrated flow và web mock dùng schema khác current canonical/claim-extraction lineage. Chúng được loại khỏi active typecheck và không được active research import.

Code được giữ để truy vết `faithfulness_validation/`, `final_explanations/` và các report lịch sử. Một số dependency nội bộ cũ có thể không còn executable độc lập.
